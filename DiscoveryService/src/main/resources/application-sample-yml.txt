server:
  port: ${PORT:8761}

spring:
  application:
    name: EUREKA-SERVER
        

eureka:
   instance:
    prefer-ip-address: false
   client:
     fetch-registry: false
     register-with-eureka: false
   server:
     enable-self-preservation: true
     eviction-interval-timer-in-ms: ${TIME_IN_MS_FOR_EVICT_STALE_SERVICES}
