server:
   port: 8083

spring:
  application:
    name: CONFIG-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#  cloud:
#    config:
#      server:
#        jdbc:
#          sql: SELECT `value` FROM `config_properties` WHERE `key` = ? AND `profile` = ? AND `label` = ?
   cloud:
    config:
      server:
        bootstrap: true  # Enables bootstrap configuration
        composite:
          - type: jdbc  # Use JDBC for database configuration
            sql: SELECT `value` FROM `config_properties` WHERE `key` = ? AND `profile` = ? AND `label` = ?
#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/