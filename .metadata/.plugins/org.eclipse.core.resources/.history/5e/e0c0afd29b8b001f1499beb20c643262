server:
   port: 8083

spring:
  application:
    name: CONFIG-SERVICE
  cloud:
    config:
      server:
        bootstrap: true  # Enables bootstrap configuration
        default-label: main  # Default branch to use
        jdbc:  # Use JDBC for database configuration
            sql: SELECT `value` FROM `config_properties` WHERE `key` = ? AND `profile` = ? AND `label` = ?
  datasource:
    url: jdbc:mysql://localhost:3306/db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
   
#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/