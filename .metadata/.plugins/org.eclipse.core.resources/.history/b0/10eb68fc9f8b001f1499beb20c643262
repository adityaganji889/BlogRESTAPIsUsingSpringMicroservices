server:
  port: 8083

spring:
  application:
    name: CONFIG-SERVICE
  cloud:
    config:
      server:
        git:
          uri: null # Disables git 
        bootstrap: false  # Disable bootstrap as we're not using Git or any other source 
        jdbc:
          sql: SELECT `value` FROM `config_properties` WHERE `key` = ? AND `profile` = ? AND `label` = ?
  datasource:
    url: jdbc:mysql://localhost:3306/db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true